import React, { useState } from 'react';
import {
	Paper,
	List,
	ListItem,
	Typography,
	ListItemText
} from '@material-ui/core';
import { RestaurantTwoTone } from '@material-ui/icons';
import { dishesDefault } from '../../store';

const Dishes = () => {
	const [dishes, setDishes] = useState(dishesDefault); //eslint-disable-line
	return (
		<Paper style={{ height: '100%' }}>
			<div>
				<RestaurantTwoTone />
				<Typography variant="h5">Rodzaj dania</Typography>
			</div>
			<List>
				{dishes.map(dish => (
					<ListItem button key={dish}>
						<ListItemText primary={dish.toLocaleUpperCase()} />
					</ListItem>
				))}
			</List>
		</Paper>
	);
};

export default Dishes;
